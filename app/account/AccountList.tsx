import React from 'react';
import { useAccounts } from '../../pages/api/useAccounts';

const AccountList = () => {
  // const { data: accountList } = useAccounts();
  // console.log(accountList, 'data');

  return <div>account</div>;
};

export default AccountList;
